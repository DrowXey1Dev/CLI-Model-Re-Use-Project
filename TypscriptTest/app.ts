console.log("Hello, World!");
// Prevent the console from closing immediately
console.log("Press any key to exit...");
process.stdin.resume(); // Keeps the process running until the user presses a key
